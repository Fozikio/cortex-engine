node_modules/
dist/
public/
*.tsbuildinfo
.env
.env.*
# Glob covers the -wal and -shm sidecars. `cortex.db` alone did not, which
# is how both were committed in 7e95ee9 and stayed tracked.
cortex.db*
demo*.gif
demo*.yml
terminalizer.yml

# ClawHub generates skill-card.md server-side; it cannot be published directly
# and must not be tracked here or it will drift from what ClawHub renders.
skills/*/skill-card.md

# Plugin Inspector output - regenerated by `clawhub package validate`
openclaw-plugin/reports/
